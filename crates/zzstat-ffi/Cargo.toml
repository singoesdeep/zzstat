[package]
name = "zzstat-ffi"
version = "0.5.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
zzstat = { path = "../zzstat-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
libc = "0.2"
